#include<bits/stdc++.h>
using namespace std;

int main(){
    
    int num = 1;

    while(num)
    {
        switch(num)
        {
            case 1: cout<<num<<endl;
                      exit(0);
        }
    }
    return 0;
}